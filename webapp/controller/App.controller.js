sap.ui.define(["sap/ui/core/mvc/Controller"], function (Controller) {
    "use strict";
    /** App shell controller — no logic required; routing initialised by Component. */
    return Controller.extend("PO_CREATEORDER.controller.App", {});
});
