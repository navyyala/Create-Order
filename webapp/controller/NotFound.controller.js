sap.ui.define(["sap/ui/core/mvc/Controller"], function (Controller) {
    "use strict";
    /** Not-found view controller — displayed when no route matches. */
    return Controller.extend("PO_CREATEORDER.controller.NotFound", {});
});
